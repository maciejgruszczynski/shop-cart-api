default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: shop-cart-api-development

test:
  <<: *default
  database: shop-cart-api-test

production:
  <<: *default
  database: shop-cart-api-production
